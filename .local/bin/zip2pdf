#!/bin/bash

set -e

FULLPATH="$1"
FILENAME=$(basename "$FULLPATH")
BASENAME="${FILENAME%.zip}"

mkdir "$BASENAME"
unzip "$FULLPATH" -d "$BASENAME"
img2pdf "$BASENAME"/* -o "$BASENAME.pdf"
rm -r "$BASENAME"