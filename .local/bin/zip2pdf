#!/bin/bash

# Get the base name
BASENAME="${1%.zip}"

# Create directory
mkdir "$BASENAME"

# Unzip file
unzip "$1" -d "$BASENAME"

# Convert files to PDF
img2pdf "$BASENAME"/* -o "$BASENAME.pdf"

# Remove directory
rm -r "$BASENAME"
